**Редактор заметок на React с функционалом создания, удаления, редактирования, поиска**

Инструкция по запуску:
### `npm run build`

(Если потребуется):
### `npm install -g serve`

### `serve -s build`

